# GeoSift_FE
geospatial discovery engine querying multi-terabyte dataset (Overture/OSM) via a type-safe Strawberry GraphQL API; implemented a multi-layer Redis caching strategy and a MongoDB/BigQuery synchronization layer to achieve sub-second latency for real-time property attribute filtering.
